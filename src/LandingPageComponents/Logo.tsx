
import React from 'react'
import {potta_one} from '@/fonts'
const Logo = () => {
  return (
    <div>
      <span className={`${potta_one.className} text-2xl font-bold text-white`}>Schol.</span>
    </div>
  )
}

export default Logo
